# PROJECT_CONTEXT — Service Marketplace

**Project**: Service Marketplace
**Type**: Web app (client + server)
**Workspace Path**: `asset-projects/service-marketplace/`

## Purpose
Marketplace-style application with a separate `client/` frontend and `server/` backend.

## Key Paths
- `client/` — Frontend UI
- `server/` — Backend API/server
- `docs/` — Project documentation
- `scripts/` — Utility scripts
- `tests/` — Automated tests
- `dist/` — Build output

## Common Commands
Run commands from `asset-projects/service-marketplace/`.

- Install: `npm install`
- Dev: `npm run dev` (or see `package.json` scripts)
- Build: `npm run build`
- Test: `npm test`

## Docs Registry
Canonical plan/status/context are tracked in `.studio/Docs/subjects/registry.yml`.
